import { createTheme } from '@material-ui/core/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#f59c17',
      dark: '#ec8e00',
      contrastText: '#FFF0FB'
    },
    secondary: {
      main: '#FFF0FB',
      contrastText: '#050301'
    }
  }
})
