import styled from 'styled-components'
import Button from '@material-ui/core/Button'

export const SubmitButtonList = styled(Button)`
  bottom: -40px;
  margin: 0 10px;
  background-color: rgb(218 165 32);
`