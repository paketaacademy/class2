import styled from 'styled-components'

export const DivContainerBtn = styled.div`
  &&{
    align-self: start;
  }
`

export const InvalidAuth = styled.p`
  color: var(--red);
  font-family: var(--primary-font-family);
`